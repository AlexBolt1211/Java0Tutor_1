package by.traning.tutor.classes.task14;

public class PrintAccount {
    // вывод на консоль
    public static void printAccount(User user){
      //  user.getAccounts().forEach(a -> System.out.println(a.toString()));
        return;
    }
}
